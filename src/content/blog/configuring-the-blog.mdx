---
title: "Configuring the blog"
description: "Learn how to configure the blog template"
pubDate: "Oct 25 2024"
published: true
tags: ["setup"]
---

Change the values in `src/config.ts` to configure the blog to your liking, see below for more information.

## SITE

set this to the url your blog is served from, without a trailing slash,
e.g. `https://astro-blog.<your-subdomain>.workers.dev` or your own domain

## BASE

set this to the path your blog is served under. leave it empty when the blog lives at the root
of `SITE`, which is the normal case on cloudflare. only set it (e.g. `/blog`) if you serve the
blog from a subpath, and then without a trailing slash

## SITE_TITLE

will be shown in the title and meta tags and og image

## SITE_NAME

will be shown in the header, leave blank to instead show the SITE_TITLE

## SITE_DESCRIPTION

will be shown in the meta tags

## SITE_FAVICON

will be used as the icon in the header and the favicon

## NAME

used in the footer (c) \<YEAR\> \<YOUR_NAME\> - \<LICENSE\>

## BASE_COLOR

shade of gray for the background one of `gray`, `neutral`, `stone`, `zinc`, `slate`

(see [tailwind css colors](https://tailwindcss.com/docs/customizing-colors))

## ACCENT_COLOR

color of links, etc.
  one of `red`, `orange`, `amber`, `yellow`, `lime`, `green`, `emerald`, `teal`,
  `cyan`, `sky`, `blue`, `indigo`, `violet`, `purple`, `fuchsia`, `pink`, `rose`

(see [tailwind css colors](https://tailwindcss.com/docs/customizing-colors))

## SOURCE_LINK

- set this to the link to the source code of your blog, e.g. `https://github.com/flo-bit/blog-template` will be shown in the footer, leave blank to hide

## LICENSE

- used in the footer (c) \<YEAR\> \<YOUR_NAME\> - \<LICENSE\>

## SOCIAL MEDIA LINKS

set any of these to empty strings to hide the respective icon in the footer

- FACEBOOK_URL
- TWITTER_URL
- GITHUB_URL
- INSTAGRAM_URL
- LINKEDIN_URL
- YOUTUBE_URL
- SUBSTACK_URL
- EMAIL
- SHOW_RSS


## POSTS_PER_PAGE

- how many posts to show on per paginated page (also used for tag pages)

## MANUAL_DARK_MODE

- should we show the dark mode toggle?
  (otherwise, it will be based on the user's system preferences)

## SEARCH_ENABLED

- should we show the search bar?
